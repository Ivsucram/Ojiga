<%= form_for(@feedback_user) do |f| %>
  <% if @feedback_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback_user.errors.count, "error") %> prohibited this feedback_user from being saved:</h2>

      <ul>
      <% @feedback_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grade %><br>
    <%= f.number_field :grade %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
